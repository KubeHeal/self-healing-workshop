apiVersion: v1
kind: Service
metadata:
  name: workshop-docs
  namespace: workshop-docs
  labels:
    app: workshop-docs
    workshop: self-healing
    app.kubernetes.io/part-of: self-healing-workshop
spec:
  selector:
    app: workshop-docs
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
